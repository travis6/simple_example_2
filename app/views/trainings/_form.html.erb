<%= simple_form_for(@training) do |f| %>

  <%= f.input :name %>
  <%= f.association :groups, :collection => @groups, :as => :check_boxes %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
